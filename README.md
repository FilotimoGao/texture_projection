# texture_projection

本项目仿照“投影仪”工作原理，用于将二维图片投射到三维图形上。

目前程序实现了一个简单的用户界面，允许用户输入图片路径并将其加载到 OpenGL 环境中进行渲染。


## 依赖

本项目依赖以下库：

- OpenGL：用于图形渲染
- GLFW：用于创建窗口和处理用户输入
- GLAD：用于加载 OpenGL 函数指针
- ImGui：用于创建用户界面
- stb_image.h：用于加载图像文件
